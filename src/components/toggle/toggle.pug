//-
    element - элеметном чего является
    addClass - дописываем нужные классы (полностью)
    mode - модификатор блока
    state - (bool = false) - вкл/выкл
mixin toggle(options)
    if !options
        -var options = {}
    -
        var classes = ''
        classes += options.element ? ' ' + options.element + '__toggle': ''
        classes += options.addClass ? ' ' + options.addClass : ''
        classes += options.mode ? ' toggle_' + options.mode : ''
        classes += options.state ? ' toggle_on' : ''
    .toggle(class=classes)

+toggle({mode: 'min', element: 'footer', state: true})